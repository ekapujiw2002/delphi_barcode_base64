program barcode_generator;

uses
  Forms,
  uMain in 'uMain.pas' {frmMain};

{$R *.res}

begin
  Application.Initialize;
  Application.Title := 'Barcode Generator';
  Application.CreateForm(TfrmMain, frmMain);
  Application.Run;
end.
